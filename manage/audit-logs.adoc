---
permalink: manage/audit-logs.html
sidebar: sidebar
keywords: ONTAP tools audit logs, security logging, compliance audit trail, audit log retention
summary: 'Learn how ONTAP tools for VMware vSphere captures, retains, and monitors audit logs for security and compliance.'
---

= Manage audit logs for ONTAP tools
:icons: font
:imagesdir: ../media/

[.lead]
Use this task to manage audit logging behavior and operational response in ONTAP tools for VMware vSphere. Audit logs provide an event trail that shows who performed an action, what action was performed, when it occurred, and whether it succeeded or failed.

.Before you begin

* Ensure that you have administrator access to ONTAP tools Manager.
* Ensure that you have maintenance console access if you plan to change local retention settings.
* Review your organization's security and compliance requirements for retention, forwarding, and incident response.

.About this task

ONTAP tools generates audit events for the following activities:

* External REST API operations
** All non-GET REST API requests are audited, such as POST, PATCH, PUT, and DELETE.
** GET requests are not audited.
* Key internal system activities
** Discovery jobs
** AutoSupport (ASUP) bundle generation jobs
** Cleanup jobs
* Maintenance console activities
** Maintenance console operations are captured in a separate audit log.

Audit logging has the following operational limits:

* GET REST API calls are not captured in the audit log.
* Disabling audit logging is not supported.
* Audit log file size is not user-configurable.

Sensitive data handling is built in:

* Sensitive data, such as passwords, tokens, and private key material, is redacted.
* Long request and response payloads might be truncated.

.Steps

. Verify what your environment audits.
+
Confirm that your audit scope includes ONTAP tools Manager UI activity, REST API activity, scheduled jobs, and maintenance console activity.

. Configure local retention.
+
Audit logs rotate daily with a default retention period of 30 days. You can configure the retention period from 1 to 1440 days through the ONTAP tools maintenance console, or through the supported ONTAP tools configuration mechanism for your deployment.

. Optional: Forward audit logs to an external syslog destination.
+
Use syslog forwarding when you need centralized monitoring, long-term retention, or SIEM correlation. Follow your organization's standards for TLS, certificates, ports, and retention.

. Monitor integrity alerts.
+
ONTAP tools checks audit log archives for integrity every 24 hours to detect potential tampering or deletion due to malicious activity. If ONTAP tools detects that an archive has been tampered with or deleted, it raises an alert in ONTAP tools Manager.

. If an integrity alert occurs, perform incident-response actions.
.. Collect a support bundle as soon as possible.
.. Preserve exported bundles and archives according to your organization's incident-response process.
.. Review alert details in ONTAP tools Manager and contact NetApp Support if required.
. Collect the log files.
+
To collect audit logs, follow the procedure in xref:../deploy/deployment-error-codes.adoc#collect-the-log-files[Collect the log files].

== Audit event fields

Audit records include a consistent set of fields to support traceability. Typical fields include:

* Timestamp (UTC)
* Username (authenticated user identity)
* Operation (HTTP method and request URI)
* Status (success or failure and status code)
* User agent (client identity, if available)
* Request identifier, when available (for example, correlation ID)
* Job identifier, when available (for asynchronous workflows)
* Tenant or vCenter identifier, when available

== Example audit record

The following example shows the type of information captured. The exact field set can vary by workflow.

[source,json]
----
{
  "timestamp": "2026-01-29T08:21:51Z",
  "username": "administrator",
  "operation": "POST /virtualization/api/v1/vcenters/{vcguid}/traditional/datastores",
  "status": "Success 202",
  "user_agent": "Mozilla/5.0 ...",
  "correlation_id": "9ee9a89e-900d-4538-bcab-79830d4ab7cc",
  "job_id": "3416",
  "type": "audit"
}
----

[IMPORTANT]
If you have long-term retention requirements, such as regulatory retention, forward audit logs to an external syslog or SIEM destination and manage retention externally.
