---
permalink: manage/maintenance-console.html
sidebar: sidebar
keywords: ONTAP tools maintenance console, appliance system management, support bundle generation, TLS and network configuration
summary: "Manage ONTAP tools configurations, reset passwords, generate support bundles, and configure TLS settings."
---
= ONTAP tools maintenance console
:icons: font
:imagesdir: ../media/

[.lead]
The maintenance console for ONTAP tools for VMware vSphere enables you to manage application, system, and network settings. You can update administrator and maintenance passwords, generate support bundles, configure log levels, manage TLS settings, and enable remote diagnostics.

After deploying ONTAP tools for VMware vSphere, if the maintenance console is not accessible, install VMware Tools from vCenter Server. Log in using the `maint` username and the password set during deployment. Use *nano* to edit files in the maintenance or root login console.

[NOTE]
Set a password for the `diag` user when you enable remote diagnostics.

Use the *Summary* tab of your deployed ONTAP tools for VMware vSphere to access the maintenance console. Select image:../media/launch-maintenance-console.gif[Play button] to start the maintenance console.

|===
| *Console Menu*| *Options*
a|
Application Configuration
a|

. Display server status summary
. Change LOG level for ONTAP tools services
. Change cert validation flag
a|
System Configuration
a|

. Reboot virtual machine
. Shut down virtual machine
. Generate token to reset manager UI password
. Change time zone
. Increase jail disk size (/jail)
. Upgrade
. Install VMware Tools
. Update vCenter credentials
. Refresh ONTAP tools services certificates

a|
Network Configuration
a|

. Display IP address settings
. Display domain name search settings
. Change domain name search settings
. Display static routes
. Change static routes
. Commit changes
. Ping a host
. Restore default settings

a|
Support and Diagnostics
a|

. Access diagnostic shell
. Enable remote diagnostic access
. Provide vCenter credentials for backup
. Take backup

|===

//NADIAA checked